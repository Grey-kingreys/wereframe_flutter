import 'package:flutter/material.dart';

class MesPostes extends StatelessWidget {
  const MesPostes({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Mes Postes"),
    );
  }
}